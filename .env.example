# LLM Configuration (OpenAI Compatible Proxy)
# Base URL for OpenAI compatible API (e.g., OneAPI, LiteLLM, New API)
LLM_BASE_URL=http://192.168.31.23:8045/v1

# API key for the LLM service
LLM_API_KEY=sk-1e346598cc3d414b842b75d3ad72baaa

# Model name to use (depends on your proxy configuration)
LLM_MODEL=gemini-3-flash

# MCP Services
# Amap (Gaode) - Direct REST API Key (recommended, faster)
AMAP_API_KEY=your_amap_web_service_key
# Legacy Amap MCP URL (optional, no longer used for core features)
AMAP_MCP_URL=https://mcp.amap.com/sse?key=your_amap_key

# Weather MCP Service (self-hosted)
WEATHER_MCP_URL=http://localhost:8083/sse

# Xiaohongshu MCP (Streamable HTTP)
XHS_MCP_URL=http://192.168.31.121:18060/mcp

# AigoHotel MCP (Streamable HTTP + Bearer token)
AIGOHOTEL_MCP_URL=https://mcp.aigohotel.com/mcp
AIGOHOTEL_MCP_TOKEN=your_bearer_token

# 12306 MCP (Streamable HTTP) - Real train/rail schedule data
TRAIN_12306_MCP_URL=https://mcp.api-inference.modelscope.net/5ff557c4466a4e/mcp

# Server Configuration
HOST=0.0.0.0
PORT=8182
